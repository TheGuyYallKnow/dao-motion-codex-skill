interface:
  display_name: "Dao Motion"
  short_description: "Author Motion Lab node graphs in Studio"
  default_prompt: "Use $dao-motion to author or modify the selected Motion Lab node graph, then validate, stage, organize, and apply it without running Preview or Play."

policy:
  allow_implicit_invocation: false
